export type CreateSessionData = {
  startTime: string;
  endTime: string;
  studentId: string;
};

export type CreateSessionFormData = {
  selectedDate: string;
  startTime: string;
  endTime: string;
  studentId: string;
};
